name: R build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup R
      uses: r-lib/actions/setup-r@v2
    - name: Render Rmd
      run: Rscript -e "rmarkdown::render('report/analysis.Rmd')"
